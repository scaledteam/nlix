#
# Created by scaled
#

@name poweroff

KList["poweroff", number] = 1

function poweroff() {
    while (KTasks:count() > 0) {
        KTasks:popTable():clear()
    }
    
    S:clear()

    runOnTick(0)
}
