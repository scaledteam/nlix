#
# This E2 made by scaled
#

@name autostart

local AUTOSTART = array(
    "hotplugd",
    "driver-terminal",
    "driver-keyboard",
    "simple-keyboard",
    "sh"
)

#[local AUTOSTART = array(
    "hotplugd",
    "driver-egp",
    "driver-cursor",
    "driver-keyboard",
    "egp-wm"
)]#

if (Programs:count() == 0) {
    local Workspace = table()
    
    foreach (I, ProgramName:string = AUTOSTART) {
        Workspace:programStart(ProgramName)
    }
}
