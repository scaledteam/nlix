#
# Created by scaled
#

@name dlist

P2F["dlist", string] = "dlist"

function dlist() {
    local List = ""
    foreach (DeviceName, DeviceList:array = S["devices", table]) {
        List += DeviceName + ": " + DeviceList:count() + "\n"
    }
    out(List)
    end()
}
