#
# This E2 made by scaled
#

@name server-players

local Output = ""
foreach (I, Player:entity = players()) {
    Output += Player:id() + " " + Player:name() + "\n"
}
terminalPrintFast(Output)
programKill()
