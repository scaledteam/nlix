#
# This E2 made by scaled
#

@name echo

if (terminalInit()) {
    local Output = ""
    foreach (I, Arg:string = varArray("args")) {
        Output += Arg + "\n"
    }
    terminalPrint(Output)
    programKill()
}
