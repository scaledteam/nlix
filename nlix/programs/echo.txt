#
# This E2 made by scaled
#

@name echo

if (terminalInit()) {
    local Output = ""
    foreach (I, Arg:string = P["args", array]) {
        Output += Arg + "\n"
    }
    terminalPrint(Output)
    programKill()
}
