#
# Created by scaled
#

@name ls

if (terminalInit()) {
    switch (stateGet()) {
        case 0,
            afileListQuery(varArray("args")[1, string])
            stateNext()
            break
        
        case 1,
            if (afileListIsReady()) {
                terminalPrint(afileListGetList():concat("\n") + "\n")
                programKill()
            }
            break
    }
}
