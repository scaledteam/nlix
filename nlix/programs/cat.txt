#
# Created by scaled
#

@name cat

if (terminalInit()) {
    switch (stateGet()) {
        case 0,
            afileLoadQuery(varArray("args")[1, string])
            stateNext()
            break
        
        case 1,
            if (afileLoadIsReady()) {
                terminalPrint(afileLoadGetData() + "\n")
                programKill()
            }
            break
    }
}
