#
# Created by scaled
#

@name cat

if (terminalInit()) {
    switch (P["state", number]) {
        case 0,
            afileLoadQuery(P["args", array][1, string])
            P["state", number] = 1
            break
        
        case 1,
            if (afileLoadIsReady()) {
                terminalPrint(afileLoadGetData() + "\n")
                programKill()
            }
            break
    }
}
