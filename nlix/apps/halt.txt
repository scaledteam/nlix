#
# This E2 made by scaled
#

@name halt

if (ThisAppUser == ROOT_USER) {
    clearAll()
    runOnTick(0)
}
else {
    terminalPrintFast("You don't have permission for this action\n")
    appClose()
}
