#
# This E2 made by scaled
#

@name plist

local List = ""
for (I = 1, Apps:count()) {
    List += I + ", " + Apps[I, table]["user", string] + ", " + Apps[I, table]["workspace", table]:id():replace("table: ", "") + ", " + Apps[I, table]["name", string] + "\n"
}
terminalPrintFast(List)
appCloseSelf()
