#
# This E2 made by scaled
#

@name plist

local List = ""
for (I = 1, Programs:count()) {
    List += I + ", " + Programs[I, table]["user", string] + ", " + Programs[I, table]["workspace", table]:id():replace("table: ", "") + ", " + Programs[I, table]["name", string] + "\n"
}
terminalPrintFast(List)
programKill()
