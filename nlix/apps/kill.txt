#
# This E2 made by scaled
#

@name kill

local App = Apps[appS("args"):toNumber(), table]
if (App != NULL) {
    terminalPrintFast("Try kill \"" + App["name", string] + "\".\n")
    App:appClose()
}
else {
    terminalPrintFast("App don't launched!.\n")
}
appCloseSelf()
