#
# This E2 made by scaled
#

@name uptime

local Uptime = systime() - SysStartTime
terminalPrintFast(int(Uptime / 60 / 60 / 24) % 30 + " days, " + int(Uptime / 60 / 60) % 24 + ":" + int(Uptime / 60) % 60 + ":" + int(Uptime) % 60 + "\n")
appCloseSelf()
