#
# Created by scaled
#

@name estart

if (arg(1) == "") {
    out("Usage: estart [egp program name]\n")
    end()
}
else {
    local Egp = exec("egp")
    local Program = exec(arg(1))
    
    Egp["in", array] = Program["out", array] = array()
    Egp["out", array] = Program["in", array] = array()
    
    Egp["parent", table] = Program
    Program["parent", table] = Egp
    
    end()
}
