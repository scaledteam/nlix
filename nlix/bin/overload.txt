#
# Created by scaled
#

@name overload

local ArgNumber = P["args", array]:shiftString():toNumber()
if (ArgNumber != 0) {
    if (S:exists("overload")) {
        S["overload", number] = ArgNumber
    }
    else {
        Overload = ArgNumber
    }
}
else {
    out("Entered value is not a number or 0.\n")
}
end()
