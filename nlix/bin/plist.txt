#
# Created by scaled
#

@name plist

#[local List = ""
for (I = 1, Ps:count()) {
    List += I + ": " + Ps[I, table]:type() + "\n"
}
out(List)
end()]#


local Counter = P["counter", number]
local Out = ""

for (I = 1, Overload) {
    Counter++
    
    Out += Counter + ": " + Ps[Counter, table]:type() + "\n"
    
    if (Counter >= Ps:count()) {
        out(Out)
        end()
        break
    }
}

if (P:isValid()) {
    P["counter", number] = Counter
    out(Out)
}
