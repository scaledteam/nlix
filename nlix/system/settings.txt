#
# Created by scaled
#

@name settings
@persist RESET
@persist [AUTOSTART AUTOSTART_TERMINAL AUTOSTART_EGP AUTOSTART_GL]:array
@persist FONT_MONOSPACED:string
@persist TICKS_INSIDE

@inputs [Port1 Port2 Port3 Port4]:wirelink
@persist PORTS_COUNT
PORTS_COUNT = 4

SysPerf = 80

FONT_MONOSPACED = "Courier New"

TICKS_INSIDE = 10

AUTOSTART_TERMINAL = array(
    "driver-terminal",
    "driver-keyboard",
    "simple-keyboard",
    "sh"
)

AUTOSTART_EGP = array(
    "driver-egp",
    "driver-cursor",
    "driver-keyboard",
    "egp-wm"
)

AUTOSTART_GL = array(
    "driver-gl",
    "driver-cursor",
    "driver-keyboard",
    "wustdd"
)

AUTOSTART = AUTOSTART_TERMINAL

RESET = 0

