#
# This E2 made by scaled
#

@name lib-state

#[function number stateGet() {
    return varNumber("state")
}

function stateSet(State) {
    varWrite("state", State)
}

function stateNext() {
    varWrite("state", varNumber("state") + 1)
}

function statePrev() {
    varWrite("state", varNumber("state") - 1)
}

function stateShift(Shift) {
    varWrite("state", varNumber("state") + Shift)
}]#


function number stateGet() {
    return ThisProgram["state", number]
}

function stateSet(State) {
    ThisProgram["state", number] = State
}

function stateNext() {
    ThisProgram["state", number] = ThisProgram["state", number] + 1
}

function statePrev() {
    ThisProgram["state", number] = ThisProgram["state", number] - 1
}

function stateShift(Shift) {
    ThisProgram["state", number] = ThisProgram["state", number] + Shift
}
