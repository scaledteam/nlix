#
# Created by scaled
#

@name system-memory

# Local
#@persist S:table

# Shared
@outputs S:table

S = table(
    "bin" = KList,
    "etc" = table(
        "hostname" = "nlix" + entity():id()
    ),
    "run" = KTasks,
    "tmp" = table()
)
