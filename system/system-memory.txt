#
# Created by scaled
#

@name system-memory

# Local
#@persist S:table

# Shared
@outputs S:table

S = table(
    "bin" = PExist,
    "etc" = table(
        "hostname" = "nlix" + entity():id()
    ),
    "run" = KTasks,
    "tmp" = table()
)
