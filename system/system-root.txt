#
# Created by scaled
#

@name system-root

# Loal
#@persist S:table

# Shared
@outputs S:table

S = table(
    "bin" = PEnabled,
    "etc" = table(
        "hostname" = "nlix" + entity():id()
    ),
    "run" = Ps,
    "mnt" = table(),
    "tmp" = table()
)
