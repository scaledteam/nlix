#
# Created by scaled
#

@name sync
@persist SYSTEM_NAME:string

local Gtable = gTable("nlix")

if (Gtable[SYSTEM_NAME, table]["S", table]) {
    S = Gtable[SYSTEM_NAME, table]["S", table]
    Ps = Gtable[SYSTEM_NAME, table]["Ps", table]
}
else {
    Gtable[SYSTEM_NAME, table] = table(
        "S" = S,
        "Ps" = Ps
    )
}
